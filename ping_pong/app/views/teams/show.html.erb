<%= week_calendar number_of_weeks: 1, games: @games do |date, games| %>
  <%= date %>
  
  <% @games.each do |game| %>
  
    <div>
      <%= link_to game.id, game_path(game) %>
    </div>
  <% end %>
<% end %>